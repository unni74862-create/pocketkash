import { DatabaseTest } from '@/components/DatabaseTest';

const DatabaseTestPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-primary/10 via-background to-accent/20 p-4 py-8">
      <DatabaseTest />
    </div>
  );
};

export default DatabaseTestPage;
